# 2026-02-24 技术路线（真实容器 reward 与数据闭环）

## 目标

- 对 R2E-Gym Lite 的 dual 任务（fix / inject）产出可复现、可扩展的真实容器评测流程
- 用真实评测结果生成可训练数据集，并评估预测 reward 与真实 reward 的一致性

## 数据与文件

- 原始离线数据（含 dual 信息与离线评估字段）
  - `trajectories/bug_dual_lite_offline_200_merged_offline_eval.jsonl`
- 通过容器内 git diff 重建 patch（保证可 apply）
  - 输出：`trajectories/bug_dual_lite_offline_200_regen_v2.jsonl`（推荐作为主产物）
  - 脚本：`trajectories/regenerate_patches_from_docker.py`
- 将 regen 数据转换为 transition（fix/inject 各一条）
  - 输出：`trajectories/rl_transitions_regen_v2_eval.jsonl`
  - 脚本：`trajectories/convert_regen_to_transitions.py`

## 真实评测（Docker）

### 核心原则

- patch 应用失败要显式报错，不应被当作“策略无效”而折算进 reward
- 仅在“基线可评测”时计算 reward；否则记录 invalid_base，并采用软惩罚（可配置）
- 将测试筛选作为评测一等公民：优先挑出能区分 old/new 的测试集合，提高 valid_base 覆盖率

### 评测脚本与输出

- 评测脚本（按 instance 分组评测）
  - `trajectories/docker_eval_real_reward_grouped.py`
- 关键逻辑改进（已落地）
  - `git apply --check` 预检查：失败记 `real_eval_error=apply_failed`
  - 单测文件级别筛选测试集：对每个 test 文件分别在 old/new 上跑一遍，再决定是否纳入 fix/inject 的评测集合
  - `valid_base` 字段：标识是否有可评测测试集合（用于分组统计与训练 mask）
  - invalid_base 软惩罚：`--invalid_base_reward`（默认 -0.5）
  - pytest rc 融入筛选：在 counts 不可靠时，用 rc 推断 all_pass/any_fail

### 推荐运行方式

- 全量评测（需要镜像可拉取）
  - `--pull --pull_platform linux/amd64 --pull_retries ...`
- 本地镜像优先评测（避免 DockerHub 波动导致全 error）
  - 先从 transition 中筛掉本地不存在镜像，生成 localonly 输入
  - 再对 localonly 输入跑真实评测

## 相似度评估（预测 reward vs 真实 reward）

### 对齐原则

- 以 `episode_id` 作为 join key
- 真实评测输出中：
  - 若存在 `real_eval_error`：不计入相似度
  - 按 `real_eval.valid_base` 分两组统计：
    - `valid_base=True`：反映 reward “形状/趋势”是否一致（主要指标）
    - `valid_base=False`：更多反映测试覆盖率不足（主要看占比，不建议与 valid 混算）

### 指标

- MAE、RMSE
- Pearson（当真实 reward 近似常数时 Pearson 可能无意义/不可算）
- sign_agree（reward 符号一致率）

## 训练验证（小规模 smoke）

### SFT smoke（数据通路验证）

- 从 transition 采样小规模数据集，进行 1-2 step LoRA-SFT 训练跑通：
  - 脚本：`trajectories/train_qwen25_coder_lora_sft.py`
  - 输出：`lora.pt` + `train_meta.json`
  - 目的：验证数据格式、tokenizer、loss 反传与保存流程正常

## 下一步改进方向（优先级从高到低）

- 提升 valid_base 覆盖率
  - 扩充每个 instance 的候选测试集合（不要只依赖单一 test_1.py）
  - 改进筛选规则的鲁棒性（rc + 更健壮的 pytest 统计解析）
  - 对 fix/inject 分别选择最合适的测试子集（而非共享同一套 tests）
- 评测稳定性
  - 镜像预拉取、镜像镜像源/缓存，减少 pull EOF
  - 对疑似 flaky 测试增加重跑与标记
- 训练与评估闭环
  - 训练时引入 `valid_base` mask：主损失仅在 valid_base 上回归 reward
  - 单独监控 invalid_base 比例作为数据质量 KPI

