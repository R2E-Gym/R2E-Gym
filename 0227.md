# 0227 技术路线说明（R2E-Gym / SWE-bench Pro / DualReward 融合）

本文在 [readme_0221.md](file:///Users/bytedance/R2E-Gym/readme_0221.md) 的 DualReward（inject/fix 对偶奖励）设计与 [0225.md](file:///Users/bytedance/R2E-Gym/0225.md) 的 SWE-bench Pro 在线执行体系基础上，补充截至 2026-02-27 的最新工程改造与当前技术路线：目标是在真实容器/真实测试可用时优先走在线强信号，在环境不可得时以离线模拟兜底，同时让 agent 的交互轨迹更“可执行、可定位、可编辑、可复盘”。

## 0. 总目标

- 能稳定跑通 SWE-bench Pro 子集，产出合规 trajectory 与可解释 observation。
- 能在容器内定位测试/源码并修改（不陷入纯浏览循环），至少能形成“打开文件→编辑→跑测/评测”的闭环。
- reward 计算尽量等价于“指定测试子集是否通过”，并可扩展到 DualReward（fix + inject）的一致性质检。

## 1. 技术路线总览：在线为准，离线兜底，轨迹可控

### 1.1 在线执行主链路（SWE-bench Pro）

1) 读取 parquet / ds（包含 `selected_test_files_to_run`、`repo_language`、`docker_image` 等）  
2) Docker 运行环境初始化：容器常驻、/testbed 对齐、工具可执行  
3) agent 运行：产生 trajectory（thought/action/observation）  
4) reward：按 `repo_language` 选择 runner 跑指定测试子集并解析汇总  
5) 输出指标：完成率、准确率、轨迹文件

### 1.2 离线兜底链路（DualReward / AioSim）

当在线测试不可得（依赖缺失、runner 缺失、镜像不可执行）时：

1) 生成/解析 patch（含 inject_patch）  
2) 通过离线模拟器输出核心状态量：`V / T_base / T_after / pass_count / fail_count`  
3) 计算对偶奖励并做一致性质检（`R_inject + R_fix` 约束）  
4) 产出 RL 数据（transitions / DPO）或作为在线 reward 的 fallback

## 2. 在线执行体系：稳定性与可执行工具链（承接 0225）

### 2.1 容器稳定性

- 容器常驻（keepalive）：避免入口进程退出导致 `docker exec 409`。
- `/testbed` 对齐到真实 repo：若镜像内 repo 在 `/app /repo /workspace` 等路径，自动找到并链接到 `/testbed`。
- 启动 cwd 兜底为 `/`：避免 chdir 到不存在目录导致容器短命。

### 2.2 工具可执行性

- 将 `file_editor/search/execute_bash` 以 python3 wrapper 形式注入镜像：避免 shebang 指向不存在 venv。
- 降低 `file_editor` 对第三方依赖的强耦合（如缺失 `chardet` 时仍可读取文件）。

### 2.3 reward：按语言 runner + “以测试汇总为准”纠偏

- `repo_language=python`：pytest  
- `repo_language=go`：go test  
- 其它：优先 jest/mocha，否则回退 npm test  
- 若退出码非 0 但测试汇总明确“全通过”，则纠偏 reward=1，并将输出分区（SETUP/TEST）便于复盘。

## 3. 轨迹可控：让本地模型也能产生“可执行轨迹”

### 3.1 约束与清洗：工具调用必须可执行

在线 SWE-bench Pro 的 agent 采用 XML-like tool 调用格式。为了适配本地模型（如 FrogMini）偶发的格式错误，采取“先修正、后兜底”的策略：

- 丢弃明显不完整或缺关键参数的工具块（如缺 command / 缺 search_term）。
- 将常见非标准命令（如 `file_editor open`）自动改写为 `file_editor view`。
- `search` 的 `path` 强制落在 `/testbed`。
- `file_editor` 的 `path` 允许任何 `/testbed/...` 子路径（避免把打开具体文件误改成目录）。
- 若工具块不可用，才回退到安全默认（如 `file_editor view /testbed`）。

目标是让“工具执行失败”从致命错误降级为可恢复噪声，保证 trajectory 至少可继续推进并可复盘。

### 3.2 启动引导（bootstrap）：把“问题→文件”提前塞给 agent

从 `problem_statement` 提取关键词（优先 `...` 反引号片段，其次标识符），在容器内执行：

- `grep -RIn <kw> . | head -n 40`

将结果以 `[BOOTSTRAP_SEARCH:<kw>]` 附加到 user_prompt，使 agent 在第 1 步就能看到“相关文件/行号”的候选集合，减少纯目录游走。

### 3.3 轻量状态机：抑制目录循环，强制进入“定位→打开文件”

为避免本地模型反复 `view /testbed`，引入了极简的行为约束：

- 若连续多次目录 view，则强制执行一次 grep（保证产生“可跳转的候选”）。
- 一旦收集到命中文件列表，则强制打开其中一个具体文件（优先包含关键字或 `/config` 路径）。

这一层不替代模型推理，只是用于打破“目录循环”并把 agent 推入“读文件内容”的阶段。

## 4. 0227 新增：把“保底 edit”从 scratch 文件迁移到真实候选源码

此前为了验证“至少发生一次 edit”，使用了创建 `.r2egym_keepalive.py` 的保底编辑动作；它能保证 patch 生成，但不具备修复意义。

0227 的改造目标是：在打开测试文件后，强制打开 `qutebrowser/config/configutils.py`，并对其中某个唯一字符串做一次最小、可逆的 `str_replace`，让 “edit” 更接近真实修复路径。

当前策略（面向 qutebrowser 样例）：

- 触发条件：检测到已打开 `tests/unit/config/test_configutils.py`（作为“已进入问题域”的信号）
- 强制动作 1：`file_editor view /testbed/qutebrowser/config/configutils.py [1,120]`
- 解析 view 输出，选取前若干行中首个注释行作为 `old_str`
- 构造 `new_str`（对注释做最小空格改动，保证可逆且尽可能唯一）
- 强制动作 2：`file_editor str_replace` 对 `configutils.py` 执行替换

注意：这一步的目的是把 agent 推入“对真实源码文件产生 diff”的流程验证，并不保证该 diff 能修复任务本身；真正修复仍需模型在此基础上继续定位、编辑与跑测。

## 5. DualReward 融合路线（承接 readme_0221 + 0225）

### 5.1 统一抽象：从在线评测抽取 DualReward 的最小状态量

无论在线/离线，都将一次任务压缩为：

- `V`：patch 是否可应用
- `T_base`：基线测试状态（fix: 有失败；inject: 全通过）
- `T_after`：应用后测试状态（fix: 全通过；inject: 有失败）
- 可选：`pass_count/fail_count`（用于步级奖励）

### 5.2 在线优先级

- fix：按 `selected_test_files_to_run` 跑指定测试子集，解析汇总得到 `T_base/T_after`
- inject：生成 inject_patch，同样跑测试得到 `T_base/T_after`
- 使用对偶一致性（`R_inject + R_fix`）做质量质检，定位 reward 噪声与异常样本

### 5.3 离线兜底优先级

当在线 runner/依赖不可得：

- 记录不可得原因（`online_unavailable_reason`）
- AioSim 输出核心状态量（`V/T_base/T_after`）
- 计算 DualReward，并与在线样本对比验证模拟准确性与一致性

## 6. 用 Claude 真实轨迹监督 Frog 的 GRPO 训练路线

本节描述如何利用 Claude 的真实运行轨迹（trajectory_steps/action/observation/最终测试结果）为 Frog14B 提供可计算的训练信号，采用 GRPO 风格（同一 state 下采样一组候选 rollouts，按 reward 计算组内优势更新），而非 DPO 的 chosen/rejected 配对。

### 6.1 Claude 轨迹在 GRPO 中的作用

- 提供高质量状态分布：问题描述、repo 上下文、工具交互历史与真实 observation 形态。
- 提供过程信号证据：是否打开具体文件、是否发生 edit、是否跑测试、是否陷入目录循环、是否因环境错误（如 409）不可用。
- 提供可验证结果：最终 reward/测试输出/patch（用于终局 reward 与过程质检）。

### 6.2 轨迹标注：从 action/observation 自动打过程标签

只依赖现有字段即可生成两类标签，既用于过滤与加权，也可作为过程判别器/多任务头监督信号。

- Episode 级标签（用于过滤/加权与统计）
  - `env_unavailable`: 任意 step observation 含 `409 Client Error: Conflict` 或长期 `Exit code: -1` 等环境层失败
  - `opened_specific_file`: 存在 `file_editor view` 且 `path` 形如 `/testbed/...` 且不等于 `/testbed`
  - `did_edit`: 存在 `file_editor` 且 `command` 属于 `str_replace/insert/create/undo_edit`
  - `ran_tests`: 存在 `execute_bash` 且 cmd 命中 `pytest|go test|npm test|yarn test|pnpm test|jest|mocha`
  - `has_patch`: `output_patch` 非空（或编辑成功的 observation 特征）
  - `dir_view_streak_max/dir_view_count`: 连续/累计目录 view 强度（用于识别“卡在浏览”）
- Step 级标签（用于过程 reward shaping）
  - `is_env_error_409/is_error`: observation 中的关键错误特征
  - `is_dir_view/is_file_view`: 是否在看目录、是否打开具体文件
  - `is_edit/is_test_cmd`: 是否编辑、是否执行测试命令

实践上建议先把 `env_unavailable` 的 episode 过滤掉或显著降权，避免把环境噪声当作策略失败信号。

### 6.3 GRPO 的 state 与 group 采样方式

- state（x）推荐用 step-level 形式起步：从 Claude 轨迹中抽取“到第 t 步为止”的上下文（problem_statement + 工具规范 + 最近 N 步 action/observation + steps remaining），作为 Frog 的输入 state。
- group（候选）在同一 state 上从 Frog 采样 M 条候选动作或短 rollout（M=4/8/16；rollout 长度 K=1~3），并在环境中真实执行获得 observation。
- 组内比较：对同一 state 的 M 个候选计算 reward 后做组内标准化，得到 advantage，用于 GRPO 更新。

这种做法能把训练集中在“下一步如何推进”上，尤其适合纠正反复 `view /testbed` 的循环策略。

### 6.4 reward 设计：终局 reward + 过程 shaping

reward 建议分层组合，在线为准，离线可兜底：

- 终局 reward（强信号）
  - 使用 SWE-bench Pro 的测试结果（按 `selected_test_files_to_run` + runner 汇总解析），得到通过/不通过或更细的 pass/fail 计数。
- 过程 shaping（弱信号但密集）
  - `r_env`: 环境错误（409/exec 不可用）强惩罚，并标记 episode 不可用
  - `r_open`: 打开具体文件正奖励
  - `r_edit`: 编辑成功正奖励（基于 observation 文本特征）
  - `r_test`: 执行测试并产出汇总正奖励
  - `r_loop`: 目录 view 连续次数递增惩罚
- 组合：`R_total = R_final + λ * Σ r_step`，λ 取较小值以保持“最终测试通过”主导。

### 6.5 过滤与加权：让 Claude 数据更有效

- Episode 过滤：`env_unavailable` 且有效 step 比例很低的 episode 直接剔除或权重置零。
- Step 降权：环境错误 step（409/Exit code -1）降权，重复低信息动作（长目录 view streak）降权，只保留关键转折点（首次打开文件/首次编辑/首次跑测）。
- Episode 加权：若有 reward 字段，reward=1 的 episode 权重更高；同时乘以过程质量系数（有效 step 比例、是否跑测等），避免“成功但过程全是环境噪声”的误导样本。

### 6.6 与 DualReward 的关系

- GRPO 的在线终局 reward 与 DualReward 的 `T_base/T_after/V` 可共享同一套结构化抽取逻辑。
- 当在线测试不可得时，可用 AioSim 输出的 `V/T_base/T_after` 产生弱终局信号，保证训练覆盖面，同时用对偶一致性做样本质检与降权。

## 7. 当前主要瓶颈与后续建议

- 本地模型能力/对齐不足时，仍可能出现“打开文件但不编辑/不跑测”的策略缺失，需要更强的流程约束或更强模型。
- `file_editor str_replace` 需要 `old_str` 在文件中唯一；因此必须先 view 足够精确的片段，或改用更稳健的“按行号替换/patch apply”工具能力。
- `search` 工具输出为 “(N matches)” 时缺行号，建议默认改用 `grep -RIn` 类命令获取行号，提升“从搜索跳到代码行”的效率。

下一步建议（按优先级）：

1) 让 agent 在打开源码后自动跑一次 `selected_test_files_to_run`（或最小 smoke test），将“跑测输出”纳入决策闭环  
2) 将 “强制 edit” 从“注释空格改动”升级为“基于 failing test 的最小修复尝试”（例如优先修改 configutils 的解析/边界处理逻辑）  
3) 将在线 reward 的结构化字段写入 trajectory（`runner_used / summary_parsed / pass_count / fail_count`），便于与 DualReward/AioSim 对齐  
