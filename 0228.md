

# 0228 技术路线说明（合并 0227 + 0228 实践改造）

本文基于 [0227.md](file:///Users/bytedance/R2E-Gym/0227.md) 的总体技术路线（SWE-bench Pro 在线为准、DualReward/AioSim 离线兜底、轨迹可控与可复盘），合并 2026-02-28 当天的工程实装与踩坑修复，形成一份“可以直接落地跑”的完整路线说明。

核心变化：0228 把 0227 的“设计目标”进一步落到工程细节，包括 API 调用模式、容器工具可执行性、SWE-bench Pro reward 真跑测试、preflight 提前终止不可评测样本、强制闭环跑测、以及显著降低远程 API 的 prompt token 开销。

---

## 0. 总目标（保持 0227 不变）

- 能稳定跑通 SWE-bench Pro 子集，产出合规 trajectory（thought/action/observation）与可解释 test_output。
- 能在容器内形成闭环：定位→打开文件→编辑→跑测/评测。
- reward 尽量等价于“指定测试子集是否通过”，并能抽取为 DualReward 统一状态量：`V / T_base / T_after`。
- 当在线不可用（缺依赖/runner/镜像环境不支持）时，明确标记 `E=0`（不可评测）并兜底离线模拟。

---

## 1. 0228 在线执行主链路（SWE-bench Pro）

### 1.1 主链路

1) 读取 parquet / ds（关键字段：`repo_language`、`selected_test_files_to_run`、`docker_image`）  
2) 初始化容器环境（/testbed 对齐、工具注入可执行、容器常驻）  
3) agent 交互产生 trajectory（步骤内含工具调用与输出）  
4) reward：在容器内按 `repo_language` 跑指定测试子集并解析汇总  
5) 输出 jsonl 与统计指标（完成率/可评测率/准确率）

脚本入口仍是 [run_swebenchpro_subset.py](file:///Users/bytedance/R2E-Gym/scripts/run_swebenchpro_subset.py#L57-L124)。

### 1.2 SWE-bench Pro 镜像 reward 真实跑测（0228 实装）

0227 目标是“reward=指定测试子集是否通过”。0228 把它落到 SWE-bench Pro 镜像（`jefzda/sweap-images`）上：不再短路返回 0，而是实现 runner 并产出 test_output。

- 入口：`DockerRuntime._calculate_reward` 对 sweap-images 分支改为调用 `_calculate_reward_swebenchpro`  
  - [docker.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L1303-L1314)
- Python：使用 pytest 跑 `selected_test_files_to_run`，并做跨版本稳定的 warning 处理  
  - `PYTEST_ADDOPTS=` 清空环境注入的 pytest 参数  
  - `PYTHONWARNINGS=default` 避免全局把 warning 升级为 error  
  - `-W ignore::pytest.PytestWarning` 避免 `PytestRemovedIn8/9Warning` 类名不存在导致 pytest 启动崩溃  
  - [docker.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L1223-L1231)
- Go：`go test`（若缺 go 则 preflight 标记不可评测）  
  - [docker.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L1214-L1222)
- Node/TS：优先 jest/mocha，否则回退 `npm test --`  
  - [docker.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L1232-L1247)

注意：目前仍保留了“以测试汇总为准”的纠偏逻辑（exit_code 非 0 但汇总显示全通过→reward=1），见 [docker.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L1255-L1274)。后续建议补充强负向规则（出现 `failed/failing/assertions failed` 则不允许判通过）以避免误判。

---

## 2. 细粒度 reward 抽象：E/V/T（三段式，可与 DualReward 对齐）

为对齐 0227 的 DualReward 抽象（`V/T_base/T_after`），0228 推荐把在线 reward 分解为三段式过程标签：

- **E（Evaluable，可评测性）**：runner/依赖可用且能运行指定测试子集  
  - E=0（不可评测）：典型如 `missing_go`、`missing_node`、`missing_python`
- **V（Valid Patch，patch 可应用性）**：是否产生合法 diff（`diff --git`）且非空  
- **T（Test Summary，测试汇总）**：测试是否明确通过（从汇总与 exit_code 解析）

对 SWE-bench Pro 镜像的 E=0，0228 不再让模型继续“瞎跑”，而是在 agent 启动前就提前终止，详见下一节。

---

## 3. 轨迹与环境可控（0228：减少冗余步 + 强制闭环）

### 3.1 preflight 提前终止不可评测（missing_go 不再耗费 LLM token）

新增 `DockerRuntime.swebenchpro_preflight()`：在 agent 真跑之前就检查关键 runner 是否存在，若缺失则直接返回一条空轨迹并将 `reward=None`、`test_output` 标记为 `[PREFLIGHT] reward_unavailable: ...`。

- preflight 实现：[docker.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L1276-L1301)
- agent 启动前调用并提前返回 Trajectory：[agent.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/agent/agent.py#L675-L706)

这一步的收益：
- 避免 missing_go 这类样本占用 10~25 step 的 API 预算
- 统一把这类样本打为 `E=0`，便于训练/统计时过滤或走离线兜底

在claude运行过程中先检验模型各步的可用性 只有当镜像环境可以真实搭建（如不会出现缺少go的条理）当成真实条例 在将这些真实条例汇总成为训练小模型的数据集。
### 3.2 环境信息自动注入 prompt（减少前 1~3 步的 ls/find）

agent 在构造 `user_prompt` 时追加 `[ENV]` 段，显式给出：
- repo_root（/testbed）
- repo_language
- selected_test_files_to_run
- 可选：/testbed 的真实解析路径（readlink/realpath）

实现位置：[agent.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/agent/agent.py#L800-L834)。

### 3.3 强制闭环：首次 edit 后自动触发一次 selected tests

当检测到首次 edit 成功（`did_any_edit=True`）后，下一轮会自动插入一次 `execute_bash` 跑测试（按 repo_language 选 pytest/go test/npm test），避免“改了但不验证”导致的 reward 噪声与训练数据低质。

实现位置：[agent.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/agent/agent.py#L998-L1030)。

---

## 4. 容器稳定性与工具可执行性（0228：修复 fail to run command / exit code 127）

### 4.1 工具脚本 shebang 不可用：改为 python3 wrapper 注入

SWE-bench Pro 镜像内通常不存在 `/root/.venv/bin/python`，导致 `file_editor` 等工具脚本执行失败。0228 将 `.py` 工具注入改为“复制 .py + 生成 /usr/local/bin/<tool> wrapper”，强制 `python3 tool.py "$@"`。

实现位置：[env.py:add_commands](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/environment/env.py#L85-L155)。

### 4.2 容器内缺少 timeout 命令：运行时探测并降级

一些镜像缺 `timeout`（coreutils 不全），此前会导致 `exit code 127`。0228 在 `DockerRuntime.run()` 中探测容器内是否存在 `timeout`，不存在则不再包裹 `timeout <sec> ...`，避免命令直接失败。

实现位置：[docker.py:run](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py#L776-L823)。

---

### 6.1 主要成本来源

在现有轨迹统计中，成本主要来自 **prompt tokens（历史上下文）**，而不是 completion。典型现象是每步 prompt token 破万，且随着 step 增长不断膨胀。

### 6.2 具体降耗措施（已实装）

1) **截断历史中的 observation**：对写入 history 的 obs 做按字符截断，默认上限 `R2EGYM_HISTORY_MAX_CHARS=6000`，避免把长日志带入后续每次请求  
   - 实现位置：[agent.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/agent/agent.py#L641-L648)、写入 history：[agent.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/agent/agent.py#L1188-L1214)
2) **缩短强制 grep 输出**：`head -n 40` 改为 `head -n 20`，减少一次 search 就产生大块 obs  
   - 位置同 agent 内的强制搜索逻辑
3) **精简非 fn-calling 的 system/instance prompt**：移除冗长教程式 prompt，保留工具格式与最小约束（减少固定前缀 token）  
   - [edit_non_fn_calling.yaml](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/config/r2egym/edit_non_fn_calling.yaml)

### 6.3 建议参数（运行侧）

- `--max-steps` 建议从 25 降到 15（先看成功率损失，再权衡成本）
- `--max-tokens` 建议设置为 4096/8192/16384，而不是 65536/80000（避免偶发超长输出拉高成本）

---

## 7. 与 0227 的合并关系：哪些目标已落地，哪些仍待补齐

### 7.1 已落地（0228 实装完成）

- SWE-bench Pro 镜像 reward 真跑测试，并产出 test_output（从“固定 0”修复为“可解释强信号”）。
- missing_go 等不可评测样本 preflight 提前终止，节省 step 与 API 成本。
- 工具链可执行性（shebang wrapper）与容器命令执行（timeout 缺失降级）修复。
- 轨迹闭环：首次 edit 后强制跑 selected tests，减少“改了不测”的轨迹噪声。
- prompt/token 成本可控：history 截断 + prompt 精简。

### 7.2 仍待补齐（建议写入后续 TODO）

- **JS/Node 汇总纠偏误判**：需要补充强负向规则（出现 failed/failing/assertions failed 等则禁止判通过），并将解析结果结构化落盘到 trajectory。
- **V（patch 可应用）与 reward 的耦合**：建议在 reward=1 前增加必要条件（例如 patch 非空或至少发生 edit），避免出现“未行动但测试本来就通过→reward=1”的样本污染训练。
- **结构化落盘**：将 `E/V/T`、runner_used、summary_parsed、pass_count/fail_count 写入 trajectory 字段，直接对齐 DualReward 状态量（减少后处理反解析）。

