# 0225 技术路线汇总（R2E-Gym / SWE-bench Pro）

本文汇总截至 2026-02-25 在本仓库中为跑通 SWE-bench Pro、稳定产出轨迹、执行测试并计算准确率所做的关键技术路线与实现点（含原因、验证方式与后续建议）。

## 0. 目标与现状

### 目标
- 让 `run_swebenchpro.sh` 能稳定运行 SWE-bench Pro 子集（默认 10%），产出合规的 trajectory（function-call XML 格式）与可用的 observation。
- 在不同语言/不同仓库镜像中，能够执行对应的测试子集（`selected_test_files_to_run`），并据此计算 reward。
- 在任务结束后输出可解释的统计：完成率、准确率等。

### 现状摘要
- 轨迹输出格式已稳定（`<function=...><parameter=...>...</function>`）。
- Docker exec 409/容器短命问题已修复。
- `/testbed` 路径已对齐到镜像内真实 repo（常见为 `/app`）。
- 工具脚本可在非 Python 镜像内运行（使用 python3 wrapper 而非固定 venv shebang）。
- reward 已升级为按 `repo_language` 选择 runner，并可在“测试全通过但后置步骤失败”时纠正误判。
- 新增子集运行脚本与准确率计算。
- 已补齐与 [readme_0221.md](file:///Users/bytedance/R2E-Gym/readme_0221.md) 的对偶奖励（inject/fix）结合思路与落地路线（见第 6 节）。

## 1. 轨迹格式检查与有效性判定

### 轨迹格式（action）
- 合格标准：每个 step 的 `action` 必须是 XML-like function call：
  - 以 `<function=...>` 开头、以 `</function>` 结尾
  - 包含 `<parameter=...>` 参数节点
- 现有检查脚本会同时验证 observation 是否是“真实执行输出”，而不是 409/缺依赖/用法错误占位。

### 有效轨迹（执行语义）
- 仅“格式合格”不等于“可执行轨迹”。
- 典型无效原因：
  - `docker exec` 在容器非 running 时触发 `409 Conflict`
  - workdir 指向不存在的 `/testbed` 导致 exec 失败
  - 工具脚本在镜像内无法执行（shebang 指向不存在的 python）

## 2. Docker 运行稳定性：修复 409 exec / 容器短命

### 根因
- 多个 SWE-bench Pro 镜像缺少 `/bin/bash` 或默认入口进程迅速退出，导致后续 `exec` 触发 `409 Conflict ("container ... is not running")`。
- 环境默认工作目录是 `/testbed`，但许多镜像实际 repo 在 `/app`，导致 chdir 失败。

### 解决策略
1) **容器常驻（keepalive）**
- RepoEnv 初始化与 reset 时，启动容器用常驻命令而非交互 bash：
  - entrypoint：`/bin/sh -c`
  - command：`sleep 3600000`
- 实现位置：
  - [env.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/environment/env.py)

2) **/testbed 自动对齐到真实 repo**
- 若容器内不存在 `/testbed`，则在 `/app /repo /workspace` 中找第一个包含 `.git` 的目录并 `ln -s` 到 `/testbed`。
- 实现位置：
  - [docker.py:setup_env](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py)

3) **容器启动工作目录兜底**
- 启动容器时默认 `working_dir="/"`，避免因为默认 cwd 不存在导致容器启动后立即失败。
- 实现位置：
  - [docker.py:start_container](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py)

### 验证方式
- 在容器内执行 `file_editor view /testbed` / `execute_bash ls`：
  - observation 返回真实文件列表（且包含 `src/`、`.git/` 等），而不是 409 或 chdir 错误。

## 3. 工具可执行性：让 file_editor/execute_bash 等在非 Python 镜像可运行

### 问题
- 工具脚本的 shebang 常指向 `/root/.venv/bin/python`，而多数 SWE-bench Pro 镜像没有该 venv。
- 导致 `timeout: failed to run command 'file_editor': No such file or directory`（实际是 interpreter 不存在）。

### 解决策略
1) **为 .py 工具生成 python3 wrapper**
- 将脚本复制为 `/usr/local/bin/<name>.py`
- 生成 `/usr/local/bin/<name>` wrapper：
  - `#!/bin/sh`
  - `exec python3 /usr/local/bin/<name>.py "$@"`
- 实现位置：
  - [env.py:add_commands](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/environment/env.py)

2) **去除 file_editor 对 chardet 的强依赖**
- `import chardet` 改为可选，缺失时用 `utf-8 + errors=replace` 读取文件。
- 实现位置：
  - [file_editor.py](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/tools/r2egym/file_editor.py)

### 验证方式
- `file_editor view /testbed` 能正常输出目录结构，无 `No module named chardet` 与无 shebang 解释器错误。

## 4. 环境初始化：避免在非 Python 镜像中做无效 venv/uv 操作

### 问题
- 早期逻辑在 `repo_language=python` 时会尝试：
  - 链接 `/testbed/.venv` 到 `/root/.venv`
  - 执行 `uv pip install ...`
- 但 SWE-bench Pro 的 Python 项目镜像未必自带 `.venv` 或 uv，导致大量噪音与失败。

### 解决策略
- 仅在确认 `/testbed/.venv/bin` 存在时才执行 venv/uv 相关步骤。
- 同时确保 `/root/.local/bin` 目录存在后再创建 python/python3 链接。
- 实现位置：
  - [docker.py:setup_env](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py)

## 5. reward 计算：SWE-bench Pro 按语言选择 runner + “以测试套件为准”纠偏

### 目标
- reward 应尽可能代表“指定测试子集是否通过”，而不是被后置步骤/环境差异误判为失败。

### 新增的 SWE-bench Pro reward 路径
- 当 `ds` 存在 `selected_test_files_to_run` 时：
  - `_calculate_reward_r2e()` 会改走 `_calculate_reward_swebenchpro()`
- 实现位置：
  - [docker.py:_calculate_reward_r2e](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py)

### runner 选择策略（按 repo_language）
- `repo_language` 含 `python`：
  - `python -m pytest -q <selected_files>`
- `repo_language` 含 `go`：
  - `go test ./...` 或 `go test <selected_files>`
- 其它（默认 JS/Node）：
  - 优先 jest：`./node_modules/.bin/jest --runInBand ...`
  - 其次 mocha：`./node_modules/.bin/mocha --timeout 20000 --exit ...`
  - 否则 `npm test --silent -- ...`

### “以测试套件结果为准”的纠偏
- 若测试命令退出码非 0，但输出中出现明确“全通过”特征，则判 reward=1：
  - mocha：`0 failing`
  - pytest：`... 0 failed ...`
  - jest：`Test Suites: X passed, X total` 且 `Tests: Y passed, Y total`
  - 或 `Ran all test suites.` 且输出不含 `failed`
- 同时将输出拆为：
  - `[SETUP]`：cd、redis、before_repo_set_cmd
  - `[TEST]`：实际测试输出
- 实现位置：
  - [docker.py:_calculate_reward_swebenchpro](file:///Users/bytedance/R2E-Gym/src/r2egym/agenthub/runtime/docker.py)

### 当前样例解释（reward 常为 0 的常见原因）
- **runner 不匹配**：不同 repo_language 需要不同 runner；旧逻辑对 Go/非 Node 容易跑不到正确测试。
- **环境依赖缺失**：例如某些镜像里无 `npm`/无 node_modules，直接导致测试命令失败。
- **后置步骤干扰**：测试通过但后置检查失败导致退出码非 0；新逻辑通过解析测试汇总纠正。
- **动作参数偶发格式错误**（非 fn-calling 模式更常见）：例如 `execute_bash` 传参写错引发脚本 usage error。

## 6. 结合 readme_0221：对偶奖励（inject/fix）与在线评测的融合路线

这一节把 [readme_0221.md](file:///Users/bytedance/R2E-Gym/readme_0221.md) 的对偶奖励设计与本次 SWE-bench Pro 在线执行体系（Docker/真实测试）合并，形成一套“在线可验证 + 离线可扩展”的奖励与数据生产路线。

### 6.1 对偶奖励的关键概念（从 readme_0221 抽象到本项目）

readme_0221 的核心是将一次 bug 修复任务拆成 **互为逆过程**的两条任务：
- **fix**：从“有 bug 的代码”出发，应用修复 patch，让测试从失败 → 通过
- **inject**：从“干净代码”出发，应用反向 patch（inject_patch），让测试从通过 → 失败

并对二者施加约束：
- **结果对称**：inject 成功与 fix 成功奖励值相等；失败惩罚也相等
- **过程互补**：步级奖励聚焦测试状态变化与 patch 有效性，逆过程形成互补
- **一致性验证**：`R_inject + R_fix` 只应落在 `{2, 0, -2}`（或按系数缩放后的集合）一类的离散集合上，用于发现异常/噪声

在本项目中，上述量可以由两种来源提供：
- **在线（Docker/真实测试）**：从真实测试执行结果提取 `T_base/T_after`（更可靠）
- **离线（AioSim/模拟）**：当环境依赖缺失/无法跑通测试时，用静态规则模拟 `T_base/T_after` 作为兜底（更广覆盖）

### 6.2 “在线为准，离线兜底”的奖励融合策略

我们当前的 SWE-bench Pro reward 已能做到“按语言选择 runner + 以测试套件汇总纠偏”，这对应 readme_0221 的 `T_after` 观测来源。下一步融合对偶奖励时，建议采用以下优先级：

1) **优先走在线真实测试（强信号）**
- fix：在镜像内按 `selected_test_files_to_run` 运行，得到 `T_base/T_after`
- inject：生成 inject_patch 后同样运行测试，得到 `T_base/T_after`

2) **在线不可得时走离线模拟（弱信号但可用）**
- 若 runner 缺失（例如 `npm` 不存在）或依赖启动失败（例如 DB 服务缺失且镜像无 server），不直接把 reward=0 作为“失败”，而是：
  - 记录 `online_unavailable_reason`
  - 退回 AioSim 输出的 `T_base/T_after/V`
  - 生成对偶奖励 `R_final`

3) **用对偶一致性做自动质检与纠偏**
- 在线执行可能出现“测试全过但后置步骤失败”的误判；我们已通过解析测试汇总做了第一层纠偏。
- 对偶一致性可以做第二层质检：
  - 若同一对（inject/fix）的 reward 和对偶约束严重冲突，则优先信任“测试汇总证据更强”的一侧，并将另一侧标为异常样本（用于过滤/降权）。

### 6.3 可落地的实现路线（分阶段）

阶段 A：**把当前在线评测结果结构化为对偶奖励输入**
- 从一次测试执行中抽取并落盘：
  - `V`：patch 是否可应用
  - `T_base/T_after`：基线与应用后测试状态（可从实际 runner 的 pass/fail 汇总得到）
  - `pass_count/fail_count`：用于步级奖励（如果 runner 能解析到数量）
- 在 trajectory 或统计侧car中写出：
  - `reward_components: {V, T_base, T_after, ...}`
  - `reward_mode: online|offline|hybrid`

阶段 B：**引入 inject_patch 生成与双向评测**
- 参考 readme_0221 的 Patch 解析步骤：为每个 fix patch 生成对应 inject_patch
- 对同一实例跑两条评测：
  - fix：原 patch
  - inject：反向 patch
- 将两条 reward 绑定为一对，并输出：
  - `dual_consistency_ok`（是否满足对偶约束）
  - `dual_consistency_bucket`（2/0/-2 等）

阶段 C：**接入离线 AioSim 作为 fallback（减少环境不一致的奖励噪声）**
- 对在线失败但“失败原因是环境/依赖缺失”的样本，走 AioSim：
  - 重点是模拟 `V` 与 `T_base/T_after`，而不是完全复现执行日志
- 同时做 AioSim 准确性验证（readme_0221 的 4.2）：
  - 抽样对比在线真实结果与模拟结果的准确率
  - 达到门槛后再扩大 fallback 覆盖

### 6.4 具体工程改造点（与现有代码对齐）

1) **reward 输出结构化**
- 当前 `_calculate_reward_swebenchpro()` 已将输出拆为 `[SETUP]` 与 `[TEST]`，建议下一步进一步拆出：
  - `[EXTRA]`：测试后置检查（如果存在）
  - 结构化字段：`tests_passed_by_summary`、`exit_code_raw`、`runner_used`

2) **抽取测试汇总（为 T_after 提供可解释证据）**
- 在现有 “以测试套件为准” 的逻辑中，继续扩充对各 runner 的汇总解析：
  - pytest：提取 `collected/failed/error` 数量
  - jest：提取 `Test Suites/Tests` 的 passed/total
  - mocha：提取 `passing/failing` 数量
  - go test：提取 `FAIL`/`ok` 的 package 数量（可选）

3) **对偶一致性统计与报警**
- 对每个样本对（inject/fix）输出一致性率，并作为 pipeline 的质量指标：
  - 低于阈值时说明 reward 噪声仍大（常见于环境不可得/解析不全/后置步骤干扰）

### 6.5 为什么这条路线符合 readme_0221 的优势诉求
- **轻量高效**：在线路径只做 runner 选择与汇总解析；离线 fallback 仅模拟核心状态量（不跑容器）。
- **对偶自洽**：对偶约束提供天然的自校验机制，避免 RL 信号互相矛盾。
- **环境无关**：当容器/依赖不完备时，AioSim 允许继续产出可用 reward，而不是把“环境失败”当成“任务失败”。
- **易维护**：新增语言/仓库类型时，只需补充 runner 选择与汇总解析规则，或在离线端补充测试状态模拟规则。

## 7. 运行子集与准确率统计：0.1 子集执行与指标输出

### 新增脚本
- [run_swebenchpro_subset.py](file:///Users/bytedance/R2E-Gym/scripts/run_swebenchpro_subset.py)

功能：
- `--frac 0.1`：按比例计算 k（当前 parquet 总数 731 → k=73）
- `--k N`：手动指定前 N 条（用于“跑满前三条后停”这种场景）
- 运行结束输出 JSON 指标：
  - `accuracy_expected = passed / expected_k`
  - `accuracy_completed = passed / completed_total`
  - `completion_rate = completed_total / expected_k`

### 入口脚本更新
- [run_swebenchpro.sh](file:///Users/bytedance/R2E-Gym/run_swebenchpro.sh)

当前默认行为：
- 要求环境变量 `OMNIMAAS_API_KEY` 已设置，否则退出
- 调用 `scripts/run_swebenchpro_subset.py` 跑 10% 子集并输出准确率 JSON

## 8. 安全与运维注意事项

### Docker Host（macOS/colima）
- 若默认 Docker Host 无法拉取镜像/走到了错误代理，可考虑显式设置：
  - `export DOCKER_HOST=unix:///Users/bytedance/.colima/default/docker.sock`

## 9. 后续建议（可选增强）
- 更精细的 runner 判定：不仅看 `repo_language`，也根据 `selected_test_files_to_run` 的文件后缀/路径（例如 `.js/.py/.go`）自动选择。
- 进一步结构化记录测试信息：把 setup/test/extra 分段写入 trajectory 字段，便于事后统计“测试是否通过”与“后置失败原因”。
- 对 JS/Node 增加依赖准备：若缺 `npm`/node_modules，可尝试 `npm ci`（需权衡成本）。
- 将对偶奖励落到工程：为 SWE-bench Pro 引入 inject_patch 生成、双向评测与一致性验证，并将其作为训练数据质量闸门。
